#include <stdio.h>

int main(int argc, char* argv[]) {
  int a = 5;
  int b = 5;

  int c = ++a;
  int d = b++;

  printf("c: %d, d: %d\n", c, d);
  printf("a: %d, b: %d\n", a, b);

  for (int i = 0; i < 19; ++i) {

  }
}